\chapter{\methodologyname}
\label{chap:metodologia}

\section{Dise\~no general}

El trabajo se estructura en cuatro fases principales m\'as una fase intermedia de derivaci\'on de MRS, cada una vinculada a un marco normativo de referencia. Se ejecutan 6 casos (uno por repositorio) en 4 flujos experimentales (C0--C3), produciendo 24 ejecuciones evaluadas con doble metodolog\'ia: an\'alisis autom\'atico de calidad de c\'odigo y r\'ubrica SRCI v3.

\begin{table}[htbp]
\centering
\caption{Fases de la metodolog\'ia}
\label{tab:fases}
\begin{tabularx}{\textwidth}{lXl}
\toprule
\textbf{Fase} & \textbf{Prop\'osito} & \textbf{Marco} \\
\midrule
Fase 1. Evidencia & Obtener requisitos del mundo real & IREB Elicitaci\'on \\
Fase 2. Formalizaci\'on & Requisitos con atributos normativos & IREB + ISO 29148 + INCOSE \\
Fase 2.5. MRS & Derivar prompts replicables & IREB Elicitaci\'on \\
Fase 3. Ejecuci\'on & Ejecutar 4 flujos sobre 6 casos & SpecKit SDD \\
Fase 4. Evaluaci\'on & Doble metodolog\'ia: autom\'atica + SRCI & IREB + ISO 29148 + ISO 25010 \\
\bottomrule
\end{tabularx}
\end{table}

\section{Fase 1: Extracci\'on de evidencia}

Se identifican cambios en seis repositorios \emph{Open Source} seleccionados para cubrir seis tipos de sistema software (tabla~\ref{tab:repos}). Glass y Vessey~\cite{glass1995taxonomy} establecieron que los problemas de desarrollo var\'ian sistem\'aticamente seg\'un el dominio de la aplicaci\'on, y que los estudios emp\'iricos deben controlar esta variable. Siguiendo ese criterio, cada repositorio representa un tipo distinto: Appwrite encarna el dominio transaccional, Medusa el financiero, Authentik el de identidad y seguridad, Cal.com el colaborativo, Directus el de datos y contenido, y n8n el de integraci\'on de servicios. Esta selecci\'on garantiza que los resultados no dependan de un \'unico tipo de sistema, sino que reflejen un espectro amplio de dominios, en l\'inea con la pr\'actica consolidada de estudiar repositorios \emph{Open Source} como fuente de evidencia emp\'irica~\cite{hassan2019msr}.

\begin{table}[htbp]
\centering
\caption{Repositorios seleccionados: tipo de sistema y justificaci\'on}
\label{tab:repos}
\begin{tabularx}{\textwidth}{lp{3.5cm}X}
\toprule
\textbf{Repositorio} & \textbf{Tipo de sistema} & \textbf{Justificaci\'on} \\
\midrule
Appwrite & Transaccional (BaaS) & APIs REST transaccionales, patr\'on dominante en SaaS \\
Medusa & Financiero (e-commerce) & Requisitos con integridad transaccional y flujos de pago \\
Authentik & Identidad y seguridad & RBAC (\emph{Role-Based Access Control}), sesiones, flujos de autorizaci\'on multi-protocolo \\
Cal.com & Colaborativo & Estado multiusuario, concurrencia, notificaciones \\
Directus & Datos (headless CMS) & Esquemas din\'amicos, APIs autogeneradas \\
n8n & Integraci\'on de servicios & Eventos, webhooks, orquestaci\'on de servicios \\
\bottomrule
\end{tabularx}
\end{table}

Para la selecci\'on de cambios individuales se aplica una r\'ubrica de cuatro dimensiones: trazabilidad (referencia a PR o commit con diff localizable), se\~nales estructurales (menciona superficie t\'ecnica o actor relevante), utilidad para derivaci\'on (comportamiento formalizable como requisito) y defendibilidad (inclusi\'on justificable). Se descart\'o una r\'ubrica IEEE 830 por generar rechazo casi universal sobre \emph{changelogs}. Se almacenan \'unicamente textos literales con trazabilidad m\'inima, sin redactar requisitos en esta fase.

\section{Fase 2: Formalizaci\'on de requisitos}

La evidencia se transforma en requisitos siguiendo una plantilla con 12 atributos IREB/ISO 29148 (v\'ease Anexo~\ref{anexo:plantilla}): ID, Nombre, Tipo, Descripci\'on formal, Fuente, Rationale, Prioridad, Verificaci\'on, Estado, Versi\'on, Dependencias y M\'odulo. La descripci\'on formal debe cumplir ocho reglas: obligaci\'on \'unica, f\'ormula ``El sistema deber\'a...'', verbo observable, sin t\'erminos subjetivos, condiciones cuantificables, verificable, autosuficiente y sin detalles de implementaci\'on.

Cada requisito supera un \emph{quality gate} de 12 criterios en tres bloques: identificaci\'on (R1--R4c), redacci\'on (R5--R9) y completitud (R10), con tres criterios bloqueantes: R5 (estructura formal), R7 (verbo observable) y R9 (verificabilidad).

\section{Fase 2.5: Derivaci\'on de MRS}

Cada requisito formalizado se transforma en un \emph{Minimal Requirement Seed} (MRS): un prompt m\'inimo de 3--5 l\'ineas con estructura can\'onica ``Como [ROL], quiero que el sistema [COMPORTAMIENTO] [contexto], para [MOTIVACI\'ON]''. Un MRS debe cumplir tres propiedades: suficiencia, minimalidad y replicabilidad.

\section{Fase 3: Ejecuci\'on comparativa}

Se definen cuatro condiciones experimentales (tabla~\ref{tab:flujos}), ejecutadas mediante scripts automatizados (\texttt{pipeline-*.sh}) que usan \texttt{opencode} + DeepSeek V4 Flash sobre entornos aislados (\texttt{env/} como \texttt{\$HOME} temporal).

\begin{table}[htbp]
\centering
\caption{Las cuatro condiciones experimentales}
\label{tab:flujos}
\begin{tabularx}{\textwidth}{llXl}
\toprule
\textbf{Flujo} & \textbf{Script} & \textbf{Descripci\'on} & \textbf{Pasos} \\
\midrule
C0 & \texttt{pipeline-c0.sh} & \textsc{SpecKit} vanilla (\emph{baseline}) & 4 \\
C1 & \texttt{pipeline-c1.sh} & \textsc{SpecKit} + IREB v1 & 9 \\
C2 & \texttt{pipeline-c2.sh} & \textsc{SpecKit} + IREB v2 + \emph{scope contract} & 10 \\
C3 & \texttt{pipeline-c3.sh} & MRS directo, sin \textsc{SpecKit} & 1 \\
\bottomrule
\end{tabularx}
\end{table}

\begin{description}
    \item[C0,  SpecKit vanilla:] Pipeline m\'inimo: \texttt{specify} (recibe \emph{changelog} literal) $\rightarrow$ \texttt{plan} $\rightarrow$ \texttt{tasks} $\rightarrow$ \texttt{implement}. Sin constituci\'on IREB, sin \texttt{clarify}, sin \texttt{checklist}. L\'inea base: ¿qu\'e produce \textsc{SpecKit} sin tratamiento metodol\'ogico?

    \item[C1 , IREB v1:] Pipeline de 9 pasos con IREB Kit v1: \texttt{constitution} (9 principios IREB) $\rightarrow$ \texttt{specify} (REQ con 12 atributos) $\rightarrow$ \texttt{clarify} $\rightarrow$ \texttt{checklist} $\rightarrow$ \texttt{plan} $\rightarrow$ \texttt{tasks} $\rightarrow$ \texttt{analyze} (pre) $\rightarrow$ \texttt{implement} $\rightarrow$ \texttt{analyze} (post). Sin \texttt{AGENTS.md} ni \emph{scope contract}.

    \item[C2,  IREB v2:] Pipeline de 10 pasos. A\~nade a C1: \texttt{AGENTS.md} con 5 reglas universales (no alucinar, trazabilidad, verificabilidad, minimalidad, no ambig\"uedad) y \emph{scope contract} pre-implementaci\'on que define expl\'icitamente qu\'e archivos modificar y cu\'ales no.

    \item[C3,  MRS directo:] El MRS se pasa directamente a \texttt{opencode} sin \textsc{SpecKit}. Un solo paso. Referencia extrema: ¿qu\'e ocurre sin metodolog\'ia alguna?
\end{description}

\section{Fase 4: Evaluaci\'on en dos fases}

\subsection{An\'alisis autom\'atico}

Scripts Python aplican seis herramientas sobre los 24 artefactos generados: \texttt{semgrep} (seguridad), \texttt{eslint} (calidad JS/TS), \texttt{phpcs} (estilo PHP), \texttt{shellcheck} (shell), \texttt{pyflakes} (Python) y \texttt{cloc} (l\'ineas de c\'odigo). Cada herramienta cubre una dimensi\'on del modelo ISO/IEC 25010.

\subsection{R\'ubrica SRCI v3}

Evaluaci\'on manual mediante inspecci\'on directa de los artefactos generados, con cinco bloques ponderados:

\begin{description}
    \item[Bloque A , Preservaci\'on del requisito (25\%):] ¿Conserva \textsc{SpecKit} los 12 atributos IREB en \texttt{spec.md}?
    \item[Bloque B,  Conformidad (20\%):] ¿Satisface el c\'odigo el requisito sin desviaciones?
    \item[Bloque C , Verificaci\'on y testing (12\%):] ¿Hay tests? ¿Cubren los criterios de verificaci\'on?
    \item[Bloque D,  Trazabilidad:] ¿Soportan los artefactos intermedios la trazabilidad requisito $\rightarrow$ implementaci\'on?
    \item[Bloque E,  TSR (\emph{Test Satisfaction Rate}, 43\%):] ¿Qu\'e porcentaje de requisitos satisface el c\'odigo?
\end{description}

\section{Dise\~no experimental}

El experimento compara cuatro condiciones (C0--C3) que difieren \'unicamente en la informaci\'on que recibe el agente y en la configuraci\'on del pipeline. El dise\~no permite aislar el efecto de cada componente:

\begin{itemize}
    \item \textbf{C3 $\rightarrow$ C0} mide el efecto de usar un pipeline estructurado (\textsc{SpecKit}) frente a una petici\'on directa al modelo. Es la diferencia entre tener un proceso y no tenerlo.
    \item \textbf{C0 $\rightarrow$ C1} mide el efecto de formalizar los requisitos con la plantilla IREB. Ambas condiciones usan \textsc{SpecKit}, pero C1 recibe requisitos con 12 atributos en lugar del texto literal del \emph{changelog}.
    \item \textbf{C1 $\rightarrow$ C2} mide el efecto de a\~nadir mecanismos de robustez (\texttt{AGENTS.md} y \emph{scope contract}) sobre un pipeline que ya usa requisitos formalizados.
\end{itemize}

Este dise\~no sigue la l\'ogica de la gu\'ia metodol\'ogica de referencia, que propone comparar una petici\'on informal (C0), una especificaci\'on estructurada (C1) y una especificaci\'on construida con requisitos elicitados (C2). La condici\'on C3 se a\~nade como extensi\'on para establecer un l\'imite inferior absoluto: qu\'e ocurre cuando no hay ni pipeline ni especificaci\'on.

\section{Decisiones metodol\'ogicas}

\begin{table}[htbp]
\centering
\caption{Decisiones metodol\'ogicas y alternativas descartadas}
\label{tab:decisiones}
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Decisi\'on} & \textbf{Alternativa descartada} & \textbf{Justificaci\'on} \\
\midrule
IREB como marco & IEEE~830 (SRS) & IREB es m\'as abstracto y adaptable a fuentes documentales sin \emph{stakeholders} \\
Elicitaci\'on v\'ia \emph{changelogs} & \emph{Stakeholders} reales & Inviable en un TFG. \emph{Changelogs} con trazabilidad p\'ublica verificable \\
Plantilla 12 atributos & Notaci\'on formal (Rupp, l\'ogica) & Desproporcionada. Lenguaje natural con criterios de verificaci\'on \\
Ejecuci\'on \'unica & Iterativa con intervenci\'on humana & La iteraci\'on enmascara el comportamiento nativo del pipeline \\
SRCI 3 niveles & Evaluaci\'on binaria & La granularidad ternaria enriquece el an\'alisis cualitativo \\
4 flujos (C0--C3) & 2 flujos (Flow A/B) & Permite aislar efectos: C3 (sin pipeline), C0 (pipeline solo), C1 (+IREB), C2 (+robustez) \\
\bottomrule
\end{tabularx}
\end{table}
